# 📊 채용 데이터 대시보드 (Streamlit)

## 실행 방법

```bash
# 1. 패키지 설치
pip install streamlit pymysql pandas

# 2. 대시보드 실행 (프로젝트 루트에서)
streamlit run dashboard/app.py
```

브라우저에서 자동으로 `http://localhost:8501` 이 열립니다.
